PATH=/opt/slurm/sbin:/opt/slurm/bin
MANPATH=/opt/slurm/share/man

# This display is probably more useful in BurrMill environment. But these are
# only the defaults, anyway.
SQUEUE_FORMAT='%.15i %9P %24j %11T %.8M %.6D %R'
SINFO_FORMAT='      %9P %12G %.6w %.6Y %.6D %.11t %N'
